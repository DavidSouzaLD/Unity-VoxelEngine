using System.IO;
using UnityEngine;

public class Logs : MonoBehaviour
{
    public void CreateLogs()
    {
        /*string finalPath = Directory.GetParent(Application.dataPath).FullName;

        Debug.Log(finalPath);

        if (!File.Exists(finalPath))
        {
            // Create a file to write to.
            using (StreamWriter sw = File.CreateText(finalPath))
            {
                if (Input == null)
                {
                    sw.WriteLine("Input not found!");
                }
            }
        }

        Application.Quit();*/
    }
}
